# API Backend URL
# For local development: use localhost
# For remote access: use the remote machine's IP address
VITE_API_URL=http://localhost:8001

# Example for remote access from another machine:
# VITE_API_URL=http://192.168.1.100:8001
# (Replace 192.168.1.100 with your remote machine's IP)
